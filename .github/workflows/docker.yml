name: Docker

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Docker CI is disabled
        run: |
          echo "Automated Docker build/publish is disabled."
          echo "GitHub-hosted runners are unsupported because the target platform requires a local 1C:EDT installation."
          echo "Use the local build instructions in docker/README.md."
